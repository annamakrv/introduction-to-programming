# The first program prints 3.
x = 0
3.times do
  x += 1
end
puts x

# The second program shows the error 
#   undefined local variable or method `x' for main:Object (NameError).
# Because x is available only inside the do/end block.
y = 0
3.times do
  y += 1
  x = y
end
puts x